{
  "name": "people-context",
  "version": "0.1.0",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "bun run --watch src/index.ts",
    "start": "bun run src/index.ts",
    "build": "bun build --compile --minify --sourcemap --bytecode --format=esm ./src/index.ts --outfile people-context",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "coverage": "bun test --coverage 2>&1 | tee /dev/stderr | bun scripts/check-coverage.js",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "verify": "bun run typecheck && bun run format:check && bun run lint && bun test && bun run coverage",
    "pipeline:state": "bun run scripts/pipeline/state-cli.ts",
    "pipeline:status": "bun run scripts/pipeline/dashboard-cli.ts",
    "pipeline:metrics": "bun run scripts/pipeline/metrics-cli.ts"
  },
  "devDependencies": {
    "@eslint/js": "^9",
    "@types/bun": "latest",
    "eslint": "^10",
    "eslint-config-prettier": "^10",
    "prettier": "^3",
    "typescript": "^6",
    "typescript-eslint": "^8"
  },
  "peerDependencies": {
    "typescript": "^6"
  },
  "dependencies": {
    "elysia": "1.4.28",
    "jose": "6.2.3",
    "nats": "2.29.3"
  }
}
